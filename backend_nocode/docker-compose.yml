version: '3.9'

services:
  web:
    build: .
    container_name: no_code_backend
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/usr/src/app
    ports:
      - "8030:8000"
    env_file:
      - .env 
    restart: always
